# Trip-a-Trip Android tracker

Minimal foreground location service for the Romania trip. It publishes retained GPS updates to the same MQTT room used by the web map. Build artifact: `app-debug.apk` via GitHub Actions.
